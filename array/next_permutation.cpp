// given an array of intg. nd we have to find all its permutations in lexicographically order i.e. in dictionary form
// nd then find the next permutation after the given array permutation
#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"enter n:";
    cin>>n;
    int arr[n];
    cout<<"enter array elements:"<<endl;
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
}
